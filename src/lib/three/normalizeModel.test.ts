import{describe,expect,it}from'vitest';import{Box3,BoxGeometry,Group,Mesh,Vector3}from'three';import{normalizeModel}from'./normalizeModel';describe('normalizeModel',()=>{it('centers and grounds arbitrary geometry',()=>{const g=new Group();const m=new Mesh(new BoxGeometry(10,4,6));m.position.set(8,5,-3);g.add(m);const result=normalizeModel(g,4.683),box=new Box3().setFromObject(g),center=box.getCenter(new Vector3());expect(result.size.x).toBeCloseTo(4.683,3);expect(box.min.y).toBeCloseTo(0,3);expect(center.x).toBeCloseTo(0,3);expect(center.z).toBeCloseTo(0,3)})});
