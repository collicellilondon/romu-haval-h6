import{useProgress}from'@react-three/drei';export function LoadingScreen({pending}:{pending:boolean}){const{progress}=useProgress();if(!pending&&progress>=100)return null;return<div className="loading"><div className="loadingMark">ROMU</div><div className="loadingTrack"><i style={{width:`${pending?12:progress}%`}}/></div><span>{pending?'VERIFICANDO MODELO':`${Math.round(progress)}%`}</span></div>}
