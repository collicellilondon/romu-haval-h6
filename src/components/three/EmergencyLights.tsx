import{useFrame}from'@react-three/fiber';import{useRef}from'react';import{PointLight}from'three';export function EmergencyLights({enabled}:{enabled:boolean}){const red=useRef<PointLight>(null),blue=useRef<PointLight>(null);useFrame(({clock})=>{const phase=Math.floor(clock.elapsedTime*7)%4;if(red.current)red.current.intensity=enabled&&(phase===0||phase===1)?8:0;if(blue.current)blue.current.intensity=enabled&&(phase===2||phase===3)?8:0});return<group position={[0,2.2,0]}><pointLight ref={red} position={[-.55,0,0]} color="#f0182b" distance={5}/><pointLight ref={blue} position={[.55,0,0]} color="#176aff" distance={5}/></group>}
